== README

This is a sample application to demonstrate one way of using JSON
Web Tokens to auth and store values in the session.

Tested on Heroku, you'll need to set the JWT_SECRET environment variable.
